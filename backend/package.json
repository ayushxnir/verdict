{
  "name": "backend",
  "version": "1.0.0",
  "description": "A full-stack voting poll application where users choose between two options and instantly see how the crowd decides. Built to explore real-time voting logic, rate limiting, and clean full-stack architecture using the MERN stack.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server.js",
    "start": "node server.js"
  },
  "author": "Ayush Niraula",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "mongoose": "^9.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
